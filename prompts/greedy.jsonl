{"id":"g-001","prompt":"Continue this Python function body. Output code only.\n\ndef merge_sorted(a: list[int], b: list[int]) -> list[int]:\n    out = []\n    i = j = 0\n    while"}
{"id":"g-002","prompt":"Explain in exactly three sentences why floating-point addition is not associative."}
{"id":"g-003","prompt":"Continue this Python function body. Output code only.\n\ndef rle_encode(s: str) -> str:\n    if not s:\n        return ''\n    parts = []\n    prev = s[0]\n    count = 1\n    for ch in s[1:]:"}
